cryptography
keyring
piexif
Pillow>=9.0.0
numpy
# GUI framework
PySide6>=6.6.0
# Testing
pytest
pytest-qt

# Optional dependency for ONNX-based CLIP similarity checking (Blacklist → Similarity Check tab).
# Also requires a tokenizer: pip install git+https://github.com/openai/CLIP.git
#                         or: pip install transformers  (with openai/clip-vit-base-patch32 cached locally)
# Torch CLIP is also supported automatically if 'clip' and 'torch' are already installed.
# onnxruntime>=1.16.0

# Optional dependencies for image format conversion
# Uncomment the following lines to enable image conversion capabilities:
# opencv-python>=4.5.0  # For video frame extraction (MP4, AVI, MOV, MKV, WebM, GIF)
# rawpy>=0.17.0  # For RAW camera file conversion (CR2, NEF, ARW, DNG, RAW)
# pypdfium2>=4.0.0  # For PDF first page extraction
# cairosvg>=2.5.0  # For SVG to PNG conversion
# pyppeteer>=1.0.0  # For HTML to image conversion
# pillow-avif-plugin>=1.4.0  # For AVIF conversion (registered via utils/pillow_plugins.py)
# pillow-heif>=0.16.0  # For HEIC/HEIF conversion (registered via utils/pillow_plugins.py)
# pillow-jxl-plugin>=1.3.0  # For JPEG XL conversion (registered via utils/pillow_plugins.py)